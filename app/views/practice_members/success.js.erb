$('p#flash_notice').html('<%= flash[:notice] %>')
set_new_practice_member();

// must reload the practice member selector
$('#identity_selector').html("<%= escape_javascript(render :partial => 'layouts/identity_selector') %>");

// must show the find button, this is done by modifying a var in _left_column.html.erb and running a function there
identity_selector_available = true;
set_left_column_buttons();
