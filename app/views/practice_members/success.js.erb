<%= fading_flash_notice %>

// must reload the practice member selector
$('#practice_member_selector').html("<%= escape_javascript(render :partial => 'layouts/practice_member_selector') %>");
// set the main frame to show the new practice member's travel card
$('#main_frame').html("<%= escape_javascript(render :partial => 'travel_cards/form') %>");
toggle_dialog('new_practice_member');
initialize_page();
practice_member_selector_available=true;
set_left_column_buttons();
